class Solution {
public:
    int largestElement(vector<int>& nums) {
        nums[0]= largest;
        for(int i=1; i<n;i++){
            if(nums[i]>largest){
                nums[i]=largest;
            }
        }
        return largest;

    }
};
