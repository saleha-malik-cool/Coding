# include <iostream>
# include <vector>
using namespace std;
int rotate(vector <int>& arr, int n ){
    int temp = arr[0];
    for (int j =0; j<n-1; j++){
        arr[j]=arr[j+1];

        }


    
   arr[n-1]= temp;
}
int main(){
    vector <int>arr = {1,2,3,4,5};
    int ans = rotate(arr, arr.size());
    for (int i=0; i<arr.size(); i++){
        cout<<arr[i]<<" ";
    }


}


